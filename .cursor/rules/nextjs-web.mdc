---
description: Next.js frontend conventions and patterns
globs: apps/web/**/*.{ts,tsx}
alwaysApply: false
---

# Next.js Frontend Conventions

## Tech Stack

- **UI**: shadcn/ui components + Tailwind CSS
- **State**: Zustand for global state management
- **Forms**: react-hook-form + @hookform/resolvers + Zod validation
- **HTTP**: Axios for API calls
- **Path alias**: `@/` maps to `./src/`

## Zustand Stores

Place stores in `src/stores/`. Use slices pattern for complex state:

```typescript
// ✅ GOOD
import { create } from 'zustand';

interface AuthStore {
  user: User | null;
  setUser: (user: User | null) => void;
}

export const useAuthStore = create<AuthStore>((set) => ({
  user: null,
  setUser: (user) => set({ user }),
}));
```

## Forms with Zod

Define schemas in the component or a shared schemas file:

```typescript
const formSchema = z.object({
  email: z.string().email('Invalid email'),
  name: z.string().min(2, 'Name too short'),
});

type FormValues = z.infer<typeof formSchema>;

const form = useForm<FormValues>({
  resolver: zodResolver(formSchema),
});
```

## Axios

Use a configured Axios instance from `src/lib/axios.ts`:

```typescript
import api from '@/lib/axios';
const { data } = await api.get('/users');
```

## Components

- shadcn components live in `src/components/ui/`
- Feature components in `src/components/<feature>/`
- Page-level components in `src/components/pages/`
